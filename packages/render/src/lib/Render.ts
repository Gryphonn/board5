import { ClickManager } from "./managers/ClickManager";

export class Render {
  constructor() {
    console.log("hello");
  }

  /**
   * A manager for recording multiple click events.
   *
   * @internal
   */
  //   protected _clickManager = new ClickManager(this.editor);
}
